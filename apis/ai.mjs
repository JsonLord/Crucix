export async function analyzeLogs(config, spaceId, logLines, readmeContent) {
  // LLM functionality removed for lightweight deployment
  return "AI Analysis is disabled in this lightweight deployment version.";
}
